services:
    server:
        container_name: server
        build:
            context: .
            dockerfile: ./.docker/dev/Dockerfile
        ports:
            - 8080:8080
        volumes:
            - .:/app
        stdin_open: true
        tty: true
